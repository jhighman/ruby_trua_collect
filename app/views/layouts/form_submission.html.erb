<!DOCTYPE html>
<html>
  <head>
    <title>Multi-Step Form</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-background text-foreground">
    <div class="container mx-auto px-4 py-8 max-w-7xl">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        <div class="md:col-span-1">
          <%= render_card title: "Form Progress", class: "mb-6" do %>
            <ul class="divide-y divide-border">
              <% FormConfig.step_ids.each do |step_id| %>
                <li class="p-4 <%= @step_id == step_id ? 'bg-muted' : '' %>">
                  <div class="flex items-center">
                    <% if @form_submission.step_complete?(step_id) || @navigation_state[:completed_steps].include?(step_id) %>
                      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="mr-2 h-4 w-4 text-primary">
                        <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                        <polyline points="22 4 12 14.01 9 11.01"></polyline>
                      </svg>
                    <% end %>
                    <span class="<%= @form_submission.step_complete?(step_id) ? 'text-primary font-medium' : '' %>">
                      <%= step_id.humanize %>
                    </span>
                  </div>
                </li>
              <% end %>
            </ul>
          <% end %>
        </div>
        
        <div class="md:col-span-3">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>