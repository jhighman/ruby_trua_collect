<div class="form-wizard-complete">
  <div class="form-wizard-complete-icon">
    <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
      <polyline points="22 4 12 14.01 9 11.01"></polyline>
    </svg>
  </div>
  
  <h1 class="form-wizard-complete-title"><%= t('form_wizard.complete.title', default: 'Form Completed') %></h1>
  <p class="form-wizard-complete-message"><%= t('form_wizard.complete.message', default: 'Thank you for completing the form.') %></p>
  
  <div class="form-wizard-complete-summary">
    <h2 class="form-wizard-complete-summary-title"><%= t('form_wizard.complete.summary_title', default: 'Summary') %></h2>
    
    <div class="form-wizard-complete-summary-content">
      <h3><%= t('form_wizard.complete.personal_info_heading', default: 'Personal Information') %></h3>
      <div class="form-wizard-complete-summary-section">
        <div class="form-wizard-complete-summary-item">
          <div class="form-wizard-complete-summary-label"><%= t('form_wizard.complete.first_name_label', default: 'First Name') %></div>
          <div class="form-wizard-complete-summary-value"><%= @form_submission.get_field_value(:first_name) %></div>
        </div>
        
        <div class="form-wizard-complete-summary-item">
          <div class="form-wizard-complete-summary-label"><%= t('form_wizard.complete.last_name_label', default: 'Last Name') %></div>
          <div class="form-wizard-complete-summary-value"><%= @form_submission.get_field_value(:last_name) %></div>
        </div>
        
        <% if @form_submission.get_field_value(:date_of_birth).present? %>
          <div class="form-wizard-complete-summary-item">
            <div class="form-wizard-complete-summary-label"><%= t('form_wizard.complete.date_of_birth_label', default: 'Date of Birth') %></div>
            <div class="form-wizard-complete-summary-value"><%= @form_submission.get_field_value(:date_of_birth) %></div>
          </div>
        <% end %>
      </div>
      
      <h3><%= t('form_wizard.complete.contact_details_heading', default: 'Contact Details') %></h3>
      <div class="form-wizard-complete-summary-section">
        <div class="form-wizard-complete-summary-item">
          <div class="form-wizard-complete-summary-label"><%= t('form_wizard.complete.email_label', default: 'Email Address') %></div>
          <div class="form-wizard-complete-summary-value"><%= @form_submission.get_field_value(:email) %></div>
        </div>
        
        <% if @form_submission.get_field_value(:phone).present? %>
          <div class="form-wizard-complete-summary-item">
            <div class="form-wizard-complete-summary-label"><%= t('form_wizard.complete.phone_label', default: 'Phone Number') %></div>
            <div class="form-wizard-complete-summary-value"><%= @form_submission.get_field_value(:phone) %></div>
          </div>
        <% end %>
        
        <div class="form-wizard-complete-summary-item">
          <div class="form-wizard-complete-summary-label"><%= t('form_wizard.complete.preferred_contact_label', default: 'Preferred Contact Method') %></div>
          <div class="form-wizard-complete-summary-value">
            <% if @form_submission.get_field_value(:preferred_contact) == 'email' %>
              <%= t('form_wizard.complete.preferred_contact_email', default: 'Email') %>
            <% else %>
              <%= t('form_wizard.complete.preferred_contact_phone', default: 'Phone') %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <div class="form-wizard-complete-actions">
    <%= link_to t('form_wizard.complete.start_over', default: 'Start Over'), form_submission_path, class: 'btn btn-primary' %>
    <%= link_to t('form_wizard.complete.back_to_home', default: 'Back to Home'), root_path, class: 'btn btn-secondary' %>
  </div>
</div>

<style>
  .form-wizard-complete {
    max-width: 800px;
    margin: 0 auto;
    padding: 40px 20px;
    text-align: center;
  }
  
  .form-wizard-complete-icon {
    color: #28a745;
    margin-bottom: 20px;
  }
  
  .form-wizard-complete-title {
    font-size: 24px;
    font-weight: 600;
    margin-bottom: 10px;
  }
  
  .form-wizard-complete-message {
    font-size: 16px;
    color: #666;
    margin-bottom: 30px;
  }
  
  .form-wizard-complete-summary {
    text-align: left;
    margin-bottom: 30px;
    border: 1px solid #e9ecef;
    border-radius: 4px;
    padding: 20px;
    background-color: #f8f9fa;
  }
  
  .form-wizard-complete-summary-title {
    font-size: 20px;
    margin-bottom: 20px;
  }
  
  .form-wizard-complete-summary-section {
    margin-bottom: 20px;
  }
  
  .form-wizard-complete-summary-item {
    display: flex;
    margin-bottom: 10px;
    border-bottom: 1px solid #e9ecef;
    padding-bottom: 10px;
  }
  
  .form-wizard-complete-summary-label {
    font-weight: bold;
    width: 200px;
  }
  
  .form-wizard-complete-summary-value {
    flex: 1;
  }
  
  .form-wizard-complete-actions {
    display: flex;
    justify-content: center;
    gap: 10px;
  }
  
  .btn {
    display: inline-block;
    font-weight: 400;
    text-align: center;
    white-space: nowrap;
    vertical-align: middle;
    user-select: none;
    border: 1px solid transparent;
    padding: 0.375rem 0.75rem;
    font-size: 1rem;
    line-height: 1.5;
    border-radius: 0.25rem;
    transition: color 0.15s ease-in-out, background-color 0.15s ease-in-out, border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
    cursor: pointer;
  }
  
  .btn-primary {
    color: #fff;
    background-color: #007bff;
    border-color: #007bff;
  }
  
  .btn-primary:hover {
    color: #fff;
    background-color: #0069d9;
    border-color: #0062cc;
  }
  
  .btn-secondary {
    color: #fff;
    background-color: #6c757d;
    border-color: #6c757d;
  }
  
  .btn-secondary:hover {
    color: #fff;
    background-color: #5a6268;
    border-color: #545b62;
  }
  
  @media (max-width: 768px) {
    .form-wizard-complete-actions {
      flex-direction: column;
    }
    
    .form-wizard-complete-summary-item {
      flex-direction: column;
    }
    
    .form-wizard-complete-summary-label {
      width: 100%;
      margin-bottom: 5px;
    }
  }
</style>